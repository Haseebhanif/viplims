Application is written on Codeigniter 
All the controlers are in application/controller folder
All the models are in application/models
All the views are in application/views
DB files stored in DB/visiopath_lims.sql folder 
you can change database details in application/config/database.php
Server should have composer install so application composer will install all the dependencies its self
